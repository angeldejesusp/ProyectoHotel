<Window x:Class="WPFHotel.RegistrarHuespedDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFHotel"
        mc:Ignorable="d"
        Title="Registrar Huésped"
        Height="570" Width="420"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None" ResizeMode="NoResize"
        Background="#1C1C1E">

    <Window.Resources>
        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize"   Value="13"/>
            <Setter Property="Foreground" Value="#9CA3AF"/>
            <Setter Property="Margin"     Value="0,0,0,5"/>
        </Style>
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="FontFamily"      Value="Segoe UI"/>
            <Setter Property="FontSize"        Value="14"/>
            <Setter Property="Foreground"      Value="#F0F0F0"/>
            <Setter Property="CaretBrush"      Value="#F0F0F0"/>
            <Setter Property="Background"      Value="#3A3A3C"/>
            <Setter Property="BorderBrush"     Value="#555558"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding"         Value="10,0"/>
            <Setter Property="Height"          Value="40"/>
            <Setter Property="Margin"          Value="0,0,0,14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="Center"
                                          Foreground="#F0F0F0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#2563A8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ModernComboBox" TargetType="ComboBox">
            <Setter Property="FontFamily"      Value="Segoe UI"/>
            <Setter Property="FontSize"        Value="14"/>
            <Setter Property="Foreground"      Value="Black"/>
            <Setter Property="Background"      Value="#3A3A3C"/>
            <Setter Property="BorderBrush"     Value="#555558"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height"          Value="40"/>
            <Setter Property="Margin"          Value="0,0,0,14"/>
            <Setter Property="Padding"         Value="8,0"/>
        </Style>
    </Window.Resources>

    <Border Background="#2A2A2C" CornerRadius="14"
            BorderBrush="#3A3A3C" BorderThickness="1"
            Margin="10,10,10,10" Padding="28,24">
        <StackPanel>

            <!-- Título -->
            <TextBlock Text="Registrar Huésped"
                       FontFamily="Segoe UI" FontSize="18"
                       FontWeight="SemiBold" Foreground="White"
                       Margin="0,0,0,4"/>
            <TextBlock x:Name="TxtHabitacionInfo"
                       FontFamily="Segoe UI" FontSize="13"
                       Foreground="#9CA3AF" Margin="0,0,0,20"/>
            <TextBlock Text="Nombre" Style="{StaticResource FieldLabel}"/>

            <!-- Nombre -->
            <TextBox x:Name="TxtNombre" Style="{StaticResource ModernTextBox}" Height="40" Margin="0,0,0,14"/>

            <!-- Apellidos -->
            <TextBlock Text="Apellidos" Style="{StaticResource FieldLabel}"/>
            <TextBox x:Name="TxtApellidos" Style="{StaticResource ModernTextBox}"/>

            <!-- Noches y Costo en fila -->
            <Grid Margin="0,0,0,14">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Noches" Style="{StaticResource FieldLabel}"/>
                    <TextBox x:Name="TxtNoches" Style="{StaticResource ModernTextBox}"
                             Margin="0"/>
                </StackPanel>
                <StackPanel Grid.Column="2">
                    <TextBlock Text="Costo por noche" Style="{StaticResource FieldLabel}"/>
                    <Border Background="#3A3A3C" BorderBrush="#555558"
                    BorderThickness="1" CornerRadius="8"
                    Padding="10,8" Margin="0,0,0,14" Height="40">
                        <TextBlock x:Name="TxtCostoInfo"
                    Foreground="#4ADE80" FontSize="14"
                    FontFamily="Segoe UI" FontWeight="SemiBold"/>
                    </Border>
                </StackPanel>
            </Grid>

            <!-- Estatus -->
            <TextBlock Text="Estatus del huésped" Style="{StaticResource FieldLabel}"/>
            <ComboBox x:Name="CbbEstatus"
            Style="{StaticResource ModernComboBox}"
            SelectedIndex="0" Height="25">
                <ComboBoxItem Content="Normal"/>
                <ComboBoxItem Content="Vip"/>
            </ComboBox>



            <TextBlock Text="Tipo de habitación" Style="{StaticResource FieldLabel}"/>
            <Border Background="#3A3A3C" BorderBrush="#555558"
            BorderThickness="1" CornerRadius="8"
            Padding="10,8" Margin="0,0,0,14">
                <TextBlock x:Name="TxtTipoHabitacion"
               Foreground="#F0F0F0" FontSize="14"
               FontFamily="Segoe UI"/>
            </Border>



            <!-- Botones -->
            <Grid Margin="0,8,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Content="Cancelar"
                        Click="BtnCancelar_Click"
                        Background="#3A3A3C" Foreground="#9CA3AF"
                        BorderThickness="0" Height="42"
                        FontSize="14" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    CornerRadius="9">
                                <ContentPresenter HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                <Button Grid.Column="2" Content="Registrar"
                        Click="BtnRegistrar_Click"
                        Background="#2563A8" Foreground="White"
                        BorderThickness="0" Height="42"
                        FontSize="14" FontWeight="SemiBold" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="bg" Background="{TemplateBinding Background}"
                                    CornerRadius="9">
                                <ContentPresenter HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="bg" Property="Background" Value="#1D4ED8"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>

        </StackPanel>
    </Border>
</Window>